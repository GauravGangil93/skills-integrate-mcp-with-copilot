**Title:** Add parsing tests

**Description:** Add unit tests (pytest) and HTML fixtures for the scraping and parsing utilities.

**Acceptance criteria:**
- Tests run with `pytest` and cover key parsing functions with representative fixtures.
- (Optional) Add a CI job to run tests on PRs.

**Labels:** tests
